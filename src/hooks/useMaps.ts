/**
 * @deprecated Hooks migrated to `@/engines/maps/hooks` as `useWorldMaps` and
 * `useMapPins`. This file contains no exports. Update your imports to the new
 * location. Kept as a placeholder because this sandbox cannot delete files;
 * the next clean checkout can safely remove it.
 */
export {};
